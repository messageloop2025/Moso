---
description: 数据库结构变更必须增加版本号并新增升级脚本
globs: database/**/*.py, database/**/*.md, docs/数据库*.md
alwaysApply: false
---

# 数据库结构变更规则

## 必须遵守

**任何修改数据库结构的操作都将引起数据库版本增加，并必须生成对应的数据库升级脚本。**

- 修改包括：增/删/改表、列、索引，以及会改变表结构的任何 DDL 或数据迁移。
- **禁止**在业务代码中为“兼容老版本数据库”写分支逻辑；老库在**系统启动时**通过执行升级脚本升级到当前代码所兼容的版本。
- 代码只面向“当前最新版本”的库结构；兼容性由 `database/migrations/` 下的升级脚本保证。

## 操作步骤

1. 在 `database/migrations/` 下新增脚本，命名为 `NNN_简短描述.py`，其中 NNN 为**当前最新版本号**（三位数，如 001、002）。
2. 脚本内实现 `async def upgrade(db):`，仅做「从版本 N 升级到版本 N+1」的变更。
3. 若修改了 `database/models.py` 中的 SCHEMA_SQL 或建表/初始数据逻辑，需同步考虑是否应通过新的迁移脚本对已有库做 ALTER/补数据，而不是依赖 000_initial（000 仅用于空库初始化）。

未添加对应升级脚本的数据库结构修改不得合并。
