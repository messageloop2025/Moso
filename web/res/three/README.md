# three.js（本地 vendor）

- `three.min.js` — UMD，全局 `THREE`（聊天 / 多数 HTML 报告）
- `three.module.js` — ESM（`type=module` 报告）

版本：0.160.0（MIT）。由 `manifest.json` 的 `three` 包引用；聊天页同时挂载 `/static/js/three.min.js`。
